{% extends 'layout.html.twig' %}

{% block title %}Hello ServiceController!{% endblock %}

{% block body %}
<h1>{{service.title}}</h1>
    
    {{form_start(form)}}

    {{form_widget(form)}}

    <input type="submit" value="Create" />
    {{form_end(form)}}

{% endblock %}
